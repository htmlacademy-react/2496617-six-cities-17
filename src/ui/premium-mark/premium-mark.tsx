// ^======================== PremiumMark ========================^ //

export default function PremiumMark({ className }: { className: string }): JSX.Element {
  return (
    <div className={className}>
      <span>Premium</span>
    </div>
  );
}
